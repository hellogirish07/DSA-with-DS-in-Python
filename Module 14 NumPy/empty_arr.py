import numpy as np

arr = np.array([1, 2, 3, 4, 5])
if (arr.size == 0) :
    print("Array is Empty")
else:
    print("Array is not empty")

arr2 = np.array([])
if (arr2.size == 0) :
    print("Array is Empty")
else:
    print("Array is not empty")